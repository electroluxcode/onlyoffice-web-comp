# 06 - 注意事项与支持格式

[← API 参考](./05-API参考.md) | [批注修订 →](./07-批注修订与-Word-API.md)

## 注意事项

1. **初始化顺序**：必须先调用 `initializeOnlyOffice()` 再创建编辑器。
2. **容器元素**：
   - 单实例：页面中存在 `id={ONLYOFFICE_ID}` 的节点，父级建议使用 `onlyoffice-container` 类名。
   - 多实例：每个实例使用唯一 `containerId`，父级设置 `data-onlyoffice-container-id`。
3. **文件类型**：文件扩展名需与内容一致。
4. **事件清理**：组件卸载时 `off` EventBus 监听，并 `destroy()` 编辑器实例。
5. **异步操作**：API 均为异步，使用 `await` 或 `.then()`。
6. **多实例资源隔离**：各实例独立维护 `media` 映射；图片上传通过实例级 `writeFile` 路由。
7. **容器 ID 唯一性**：多实例下 `containerId` 不可重复。
8. **保存事件选择**：需要二进制数据用 `SAVE_DOCUMENT` / `export()`；仅需保存成功提示可监听 `ONSAVE`。
9. **只读导出**：只读模式下 `export()` 直接返回缓存数据，不调用 `downloadAs`。

## 支持的文件格式

### Word 文档

- `.docx` — Word 2007+
- `.doc` — Word 97-2003
- `.odt` — OpenDocument Text
- `.rtf` — Rich Text Format
- `.txt` — 纯文本

### Excel 表格

- `.xlsx` — Excel 2007+
- `.xls` — Excel 97-2003
- `.ods` — OpenDocument Spreadsheet
- `.csv` — CSV

### PowerPoint 演示文稿

- `.pptx` — PowerPoint 2007+
- `.ppt` — PowerPoint 97-2003
- `.odp` — OpenDocument Presentation
